interface:
  display_name: "Argument Map"
  short_description: "Build a source-checked 3D argument map from a paper"
  brand_color: "#5566A8"
  default_prompt: "Use $argument-map to read this paper, build a source-checked interactive 3D argument map, and explain its main spine."

policy:
  allow_implicit_invocation: true
